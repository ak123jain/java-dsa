package OOPSinJAVA;

public class Student {

    // we create a user efined data atype for all files taki ham sab files ma data type ko inherit kar
    // sake har code ma alag se ye data type declare na karna pade but in same package
    // we can also use in diff package but we want to import the class
    public static class Students{// creating a user defined data type
        String name;
        int age;
        double percent;
    }
    public static void main(String[] args) {

    }
}
